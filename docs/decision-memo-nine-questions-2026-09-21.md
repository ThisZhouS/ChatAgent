# 决策备忘：§4 九个产品语义问题（2026-09-21）

这份文件不实现任何功能，只把 9 个待确认问题变成**可以直接勾选的选项**：每条给出「现状（代码/契约位置）」「候选方案与代价」「我的建议」。你只需回一句「1A 2B 3A…」即可，我随后按同样的纪律实现：改一处、验一处、留痕一处、提交推送。

> 现状描述均以 HEAD 代码为准（本轮核对过位置）；**未确认前我不会按任何选项改代码**。

## 1. 「用户好友」分级的主体

- **现状**：`agent-tier.ts` 的 `resolveContactTier` 判定的是**发送者**与**被召唤的 AI 账号**之间的关系（owner/confirm/chat/ignore），决定这次召唤是否被接受、可用哪些工具。
- **候选**：A. 保持「人 ↔ AI 账号」（现状）；B. 扩展为「人 ↔ 人」的人际好友，再据此推导对 AI 的权限；C. 两套并存：人际好友管可见性，AI 分级管能力。
- **建议**：**A 或 C**。B 会把两件事耦合成一件：人际好友是社交关系，AI 分级是权限，合并后无法分别调整。

## 2. 「拉黑」的作用域

- **现状**：已实现为**只作用于单聊**：`targetKind === 'member'` 才判 `blocked_by_recipient`（服务层 1259/1971），群消息不受影响（早期版本曾误伤群，已修正并有测试钉住）。
- **候选**：A. 保持只拦单聊；B. 群内也生效（不投递其消息、不被 @）；C. 与 AI 分级合并（拉黑 = ignore 级）。
- **建议**：**A**。群是多人场合，「一个人拉黑另一个人」不应让整个房间失声；C 会把社交动作变成权限动作，语义过载。

## 3. 转发是否级联撤回

- **现状**：转发是**独立消息**，只在 `metadata.forwardedFrom` 记录来源（service 981）；`recallMessage` 只处理被撤回的那条及其未投喂的入队项（1120），**不级联**。
- **候选**：A. 不级联（现状）；B. 级联撤回所有转发副本；C. 不撤回但给副本打「原消息已撤回」标记。
- **建议**：**C**。B 会撤销别人已经看到/引用过的内容（历史上不可行）；A 留了一处「撤回后副本仍完整存在」的观感问题，标记成本低、信息诚实。

## 4. 公告的范围

- **现状**：`setAnnouncement` 仅群可用（`only groups have an announcement`），≤500 字，随 `conversation_announcement` 事件广播。
- **候选**：A. 仅群（现状）；B. 群 + 单聊（每个会话一条置顶）；C. 增加组织级公告（所有会话可见）。
- **建议**：**A 或 B**；C 属于新的治理面（谁能发、如何撤、是否强制已读），要做就该单独一片。

## 5. 「队列栈」的粒度

- **现状**：入队时读最近 `contextMessages`（默认 20）条作为上下文；澄清回答走 `appendInput` 追加，历史上限 50 条（超出丢弃最旧）。
- **候选**：A. 保持「每任务固定条数上限」；B. 按 token 预算动态截断；C. 引入显式「任务栈」（同会话内多轮请求串成一条链）。
- **建议**：**B（若做长上下文）**。A 的「条数」与真实成本脱钩（20 条可能是 200 也可能是 20k token）；C 是产品语义变更，应由你的问题原文来定。

## 6. 文件安全是否包含内容扫描

- **现状**：目前只做**签名与扩展名一致**校验（fail-closed，415 + 审计 `upload.rejected`，本轮线上实测），**没有任何内容扫描**（无杀毒、无 DLP、无敏感词）。
- **候选**：A. 保持边界校验（现状）；B. 接入杀毒/DLP（需选型与离线病毒库）；C. 只做轻量文本敏感词提示（不阻断）。
- **建议**：**A 起步 + 明确写进限额**。B 需要运维投入（库更新、误报处理），属于试点后按合规要求再上的事；现在把「不扫描」写清楚比含糊承诺更安全。

## 7. 「指定窗口背景」指哪个窗口

- **现状**：`ConversationAppearance` 是**按会话**的（闭集预设或 #rrggbb），渲染为聊天室底色（contracts 260/298）。
- **候选**：A. 就是聊天室底色（现状）；B. 还要覆盖侧栏/设置等其它窗口；C. 增加「全局主题」与会话级并存，会话级优先。
- **建议**：**A + C 的显式关系**。若你的原意是 B，请说明是哪几个窗口——不同窗口的对比度约束不同（气泡配色已按底色自适应）。

## 8. 「个人 ID」的含义

- **现状**：契约里只有 `memberId`（成员标识）；联系人用 `id`，没有可对外展示/可搜索的 handle，也没有工号字段。
- **候选**：A. 「个人 ID」= 登录名（现状 id）；B. 增加可自定义、可搜索的唯一 handle；C. 增加工号/组织编号字段（用于与企业目录对齐）。
- **建议**：**B 或 C，取决于你是否需要「按 ID 找人」**。B 有命名冲突与占用/改名/防冒充一整套问题（至少一片工作量）；C 更简单但要和企业目录同步。

## 9. 「目录外授权」的形式

- **现状**：工作目录限制目前是硬约束：`assertInsideWorkRoot` / `isInside` 会拒绝目录外访问，且能力下限独立把关。没有「临时放行」通道。
- **候选**：A. 保持硬拒绝（现状）；B. 一次性、按任务、经审批的目录外授权（审批摘要绑定具体路径与动作）；C. 管理员预置的允许目录白名单。
- **建议**：**B（若要放开）+ C 作为兜底**。任何放开都必须走审批摘要绑定（路径 + 动作 + 时效），否则等于取消这条不变量——这条我不会在未确认前动。

## 回答方式

回一句形如 `1: A, 2: A, 3: C, 4: A, 5: B, 6: A, 7: A+C, 8: B, 9: B+C` 即可；不确定的可以留空，我会保持现状并在文档里标注「未决」。

我会按「答案 → 实现 → 测试与类型检查 → 更新 docs 与 Prompt → 提交推送」的顺序逐条推进，每条都是可独立回退的一小片。

## 事实核对记录（2026-09-21 追加）

写这份备忘时每条现状都按 HEAD 代码核过位置；本轮又把最容易被写错的三条复查了一遍，结果如下（**这三条决定了第 3、5 条怎么选，先确认事实再选**）：

- **撤回不级联（第 3 条的前提）**：`recallMessage`（service.ts 1120-1170）只做四件事——标记撤回、取消**该消息**未投喂的入队项、写审计、广播 `message_recalled`；**没有任何遍历转发副本的逻辑**。`forwardedFrom` 只在发送时写入（981），客户端仅在消息自身被撤回时隐藏转发块（ChatView 1813）。→ 备忘里的「不级联」成立。
- **上下文与队列上限（第 5 条的前提）**：`CHATAGENT_AGENT_CONTEXT_MESSAGES` 默认 20、范围 1-200（config.ts 165）；澄清回答走 `appendInput`，实现是 `const limit = Math.max(1, options.limit ?? 50)` + `[...history, entry].slice(-limit)`（engine.ts 174-175，**保留最新、丢弃最旧**），且任务处于终态时直接拒绝追加（`isTerminalTaskState`）。→ 「默认 20 条 / 上限 50 条」成立。
- **公告**：仅群可用、≤500 字（service.ts 678/687），随 `conversation_announcement` 广播。

