# 完整产品理念审查请求

## 元数据

- 记录 ID：2026-09-17-product-concept-review
- 日期：2026-09-17（Asia/Shanghai）
- 项目：ChatAgent / E:\ChatAI；审查基线：c851228
- 关联：2026-09-15-window-resident-agent.md、2026-09-16-continuous-iteration.md
- 存储：Markdown；本轮未发现可直接用于指令归档的已配置向量存储接口，不新增存储服务。

## 用户原文

> 重新审计，并给出到完整可应用的缺口与方向。到完整可应用的差距

> 以完整的项目理念进行审查，即项目的解决的问题与功能

## 范围与验收

- 按用户问题和功能流程审查，不局限于最近 Host 修复。
- 独立于主流聊天工具，提供员工原生聊天与本机真实 Hermes。
- 员工使用电脑时后台执行；无人操作时在明确授权范围内继续/触发工作。
- 关窗常驻，明确退出停止；不恢复守护进程或 Windows Service 要求。
- 区分已实现、局部实现、未完成和未验证；给出产品门槛，不编造百分比。
- 此次交付审计与方向，不修改业务代码、不使用真实模型凭据。

## 完成结果

- 报告：`docs/product-readiness-review-2026-09-17.md`。
- 结论：聊天与 Host 有基础，聊天交办、本机执行、文件交付、授权与可验证办公仍未统一闭环。
- 根测试：323 通过、1 失败；前端：42 通过；失败文件复跑：18 通过。
- PR-01：Host 关闭后锁清理断言在全量运行间歇性失败，单独复跑未复现，根因未确认、未修复。
- 未执行真实 Hermes 办公、安装包 GUI 或局域网两机人工验收。
- 后续：本机办公闭环 → 原生聊天交付 → 授权自主工作 → 组织试用与部署维护。

## 后续修订（2026-09-17，本轮迭代收口后）

- **PR-01 的间歇性失败已定位并加固**：`packages/agent-host/src/host-security-verify.test.ts` 里“心跳推进”的等待原本是固定 `sleep(90ms)`，全量并行时会错过 20 ms 心跳窗口；已改为带截止时间的轮询（commit `6b554f1`），随后连续两次满跑 **33 文件 / 324 用例全绿**。报告里提到的“`store.close()` 后仍能读到锁文件”这一具体症状没有单独复现，因此**保留为观察项**，未宣称已根除。
- **仓库令牌卫生**：`scripts/ui-e2e.mjs` 曾在仓库内以默认值形式携带 `alice-dev-token`（可用凭据），现改为从 `--token`/`SMOKE_TOKEN`/`Temp/e2e-member.json` 解析，缺失时由新增的 `scripts/ensure-e2e-member.mjs` 签发本机专用成员 `e2e_local`；打包后客户端 E2E 用该成员与新解析路径复跑 **38/38**。历史提交中的旧令牌仍在 git 历史内，仓库若对外公开需轮换。
- 本报告其余结论（GAP-01~08、M1–M4 顺序）未变，仍以本文档为准。
